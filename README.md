# Greatest_number
This Python program takes four numbers as input and prints the greatest among them using if-else conditional statements. Run the script and enter the values when prompted.

# Student_Pass-Fail
This Python program takes marks of three subjects and total marks as input, then checks if the student has passed by ensuring each subject mark is above 33 and the total is above 120. It displays whether the student is passed or failed.

# Spam_Message
This Python program checks whether the input text contains common spam phrases like “Make a lot of money”, “buy now”, “subscribe this”, or “click this”. If any of these phrases are found in the user’s input, it classifies the message as spam. Otherwise, it prints that the message is not spam.

# Check_length
This code checks the length of a username entered by the user. If it has fewer than 10 characters, it shows a message that the username is too short; otherwise, it says the username meets the length requirement.

# Check_Presence
This program takes a name as input and checks whether that name exists in a predefined list. If the name is found in the list, it displays a message saying the name is present; otherwise, it shows that the name is not present.

# Grade_calculator
This program takes the user’s marks as input and checks which range the marks fall into. Based on that, it displays the corresponding grade from ‘Ex’ to ‘F’ using conditional statements (if-elif-else).

# Q1.Maximum Finder

This function practice program takes three numbers as input.
It uses conditional logic inside a function to determine and print the greatest value.

# Q2.Temperature Function

This exercise demonstrates how functions can be used for unit conversion.
It converts Celsius to Fahrenheit using a mathematical formula.

# Q4.Recursive Sum

This function-based exercise calculates the sum of natural numbers.
It helps in understanding logic building for recursion and iterative approaches.

# Q5.Pattern Function

This program practices function usage with loops.
It prints a star pattern based on user input, improving control over loops.

# Q6.Length Function

This exercise focuses on using functions for simple calculations.
It converts inches to centimeters using a fixed conversion factor.

# Q7.List Function

This function demonstrates list manipulation inside a function.
It removes a user-specified element from a predefined list.

# Q8.Table Function

This practice exercise prints the multiplication table of a number.
It strengthens understanding of loops inside functions.
